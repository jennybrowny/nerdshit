// main.cpp
// Entry point for the application. Creates a Game instance and starts the main loop.

#include "/Users/jennybrown/nershit/include/game.hpp"

int main() {
    // Create the Game object, which sets up the window and resources.
    Game game;
    // Start the main game loop.
    game.run();
    // Exit the program.
    return 0;
}